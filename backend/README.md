# Backend (ASP.NET Core)

Run locally with dotnet CLI or via docker-compose in the repo root.

Environment:
- SQL_CONNECTION or ConnectionStrings:DefaultConnection to point to SQL Server (docker or Azure SQL)
- AZURE_STORAGE_CONNECTION_STRING for Blob storage (Azurite local or Azure Storage account)
